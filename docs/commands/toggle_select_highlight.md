related_commands: add_highlight set_select_highlight_type

demo_code:

for_commands:

doc_body:
Toggle select highlight mode. In this mode all selected text will automatically be highlighted with the type set using @command(set_select_highlight_type).