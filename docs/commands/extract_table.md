related_commands:

demo_code:

for_commands: extract_table extract_table_with_prompt
requires_pro: true
video_file: inregrations.mp4

doc_body:
Extract the contents of selected table.
The @command(extract_table_with_prompt) command asks the user for a natural language prompt and extracts the table according to that (for example you can say something like this: "extract the contents of this table into a latex table").