related_commands: 

demo_code:

for_commands:

doc_body:
Show a context menu below the mouse cursor. The commands shown in the menu can be configured using the following configs (depending on the object under mouse cursor):

- @config(context_menu_items)
- @config(context_menu_items_for_links)
- @config(context_menu_items_for_selected_text)
- @config(context_menu_items_for_highlights)
- @config(context_menu_items_for_bookmarks)
- @config(context_menu_items_for_overview)