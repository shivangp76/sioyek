related_commands: portal overview_to_portal

demo_code:

for_commands:

doc_body:
Edit the destination of the portal with the closest source. When @command(edit_portal) is executed, sioyek jumps to the destination of the portal, now we can move around the destination and when we are done, we execute @command(prev_state) which updates the destination with the current location and brings us back to the source of the portal.