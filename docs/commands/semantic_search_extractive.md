related_commands: semantic_search

demo_code:

for_commands:
requires_pro: true
video_file: inregrations.mp4

doc_body:
Performs a semantic search on the current document. This method produces a relatively higher quality result than @command(semantic_search) but is more expensive.