related_commands: close_overview overview_definition

demo_code:

for_commands:

doc_body:
Open the overview window to the location of reference under mouse cursor. This works even when the document doesn't have links.