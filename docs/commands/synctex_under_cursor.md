related_commands: toggle_synctex synctex_under_ruler

demo_code:

for_commands:

doc_body:
Perform a synctex search under mouse cursor (this works regardless of whether we are in synctex mode or not). The search command is configured using @config(inverse_search_command).