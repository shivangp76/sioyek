related_commands: reload

demo_code:

for_commands:

doc_body:
This is similar to the @command(reload) with the difference that here we don't immediately delete the previous renders (which causes the screen to flicker before the new document is rendered).