related_commands: move_down move_up
for_commands: move_down_smooth move_up_smooth

doc_body:
Basic movement in the current document. Holding these commands results in a smooth continuous movement in the document unlike @command(move_down) and @command(move_up).