related_commands: 
related_configs: 
type: bool

demo_code:

for_configs: use_system_theme use_custom_color_as_dark_system_theme

doc_body:
Try to use system theme for light/dark modes (might not work in some operating systems).
If @config(use_custom_color_as_dark_system_theme) is set, then we use the custom color mode instead of dark mode when system theme is set to dark.
