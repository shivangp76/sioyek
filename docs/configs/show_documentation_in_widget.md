related_commands: documentation_search
related_configs: 

type: bool

demo_code:

for_configs: 

doc_body:
Normally when showing documentation (e.g. by appending a ? in the command menu or using @command(documentation_search)) we open the corresponding location in the documentation PDF file. If this option is set we open the documentation in a widget instead.