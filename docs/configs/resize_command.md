related_commands:
related_configs: 

type: string

for_configs:

demo_code:

doc_body:
@concept(macro) commands to run when sioyek window is resized.
Example:

```
resize_command fit_to_page_width_smart
```

