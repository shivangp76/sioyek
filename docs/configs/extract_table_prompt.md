related_commands: extract_table

related_configs:

demo_code:

for_configs:

requires_pro: true

doc_body:
The plain english prompt to use when extracting table data using a large language model with @command(extract_table) command.

Example:
```
extract_table_prompt Extract the table in this image into a markdown table. Take your time and make sure the result is 100% correct. Split muticolumns into separate columns.
```
