related_commands: toggle_freehand_drawing_mode write_annotations_file

related_configs: 

demo_code:

doc_body:
Normally sioyek keeps some annotations files next to the pdf files (e.g. the freehand drawings file). If you don't want this you can specify the @config(annotations_directory) and then sioyek keeps the annotations files in that directory.
