related_commands: move_down move_up move_left move_right

related_configs: vertical_move_amount horizontal_move_amount

demo_code:

for_configs: 

doc_body:
Relative scrolling sensitivity when using touchpad or mouse wheel, this is a multiplier that modifies @config(vertical_move_amount) so the overal move amount when using touchpad or mouse wheel is `touchpad_sensitivity * vertical_move_amount`.
