related_commands: goto_highlight goto_bookmark open_prev_doc
related_configs: 
type: bool

demo_code:

for_configs:

doc_body:
If set, we use a custom prettier painter to draw the menus as opposed to the default painter.