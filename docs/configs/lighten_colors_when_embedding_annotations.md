related_commands: embed_annotations embed_selected_annotation

related_configs:
type: bool

demo_code:
for_configs: 

doc_body:
When embedding sioyek annotations to PDF annotations, if this option is set we try to make the background color of dark annotations lighter so that the text on top of them is more visible. If this option is not set, we keep the original colors of the annotations.