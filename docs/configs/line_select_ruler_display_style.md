related_commands: overview_definition overview_under_cursor
related_configs:

type: string

for_configs: highlight_style selected_highlight_style

demo_code:

doc_body:
Specifies how to render highlight annotations. Possible values are the same as @config(ruler_style). @config(highlight_style) applies to unselected highlights, while @config(selected_highlight_style) applies to the currently selected highlight.