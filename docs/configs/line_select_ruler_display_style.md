related_commands: select_ruler_text
related_configs:

type: string

for_configs: highlight_style selected_highlight_style

demo_code:

doc_body:
Specifies how to render the selected line in line select mode. Possible values are the same as @config(ruler_style).