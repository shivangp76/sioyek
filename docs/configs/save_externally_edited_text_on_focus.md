related_commands: open_external_text_editor

related_configs: 

demo_code:

for_configs: 

doc_body:
If this option is set, when editing text using @command(open_external_text_editor) command, we accept the externally edited text when the sioyek window gains focus again (otherwise the user has to manually press enter to accept the input).
